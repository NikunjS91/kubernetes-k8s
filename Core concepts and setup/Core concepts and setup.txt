kubernetes(k8s) is a orcasteration tool,
CNCF cloud native computing foundation help to develop such open source by providing resources.

monolithic vs micro-services

one service fails all fails, in micro it doesnt effect other also the cost is lower then monolithic.
can use docker for each service and to manage those service or container then we can manage it with k8s.

node - server
multinode = cluster(more then one server)
we have to manage cluster in k8s.

kubernetes architecture:
1 master node and one worker node.
master node is headquater send work to worker node.
worker node runs the docker containers, and they are managed at master node.
master node contains services:
    api server : keeps the track of work happining at worker node as well as master node.(like a team lead) 
        a communication gateway between components. meaning direct communication wont work in case need to contact the cluster can be done with api server.
    like Hiring manager we have a service called scheduler which scheduler the container at worker node
        it work is to scheduler container or pods. pods are the unit inside it containers are running.
    etcs: is the database in k8s which keep the track or scheduler task(key value pair) keep stored data of k8s in etcd.
    like project manager we have a controller manager who check everthing is working(node, pod, cluster,services)

worker node services:
    kublet is another manager who works at worker node level.check pods working or not and tells the api server and he updated the scheduler ,etcd and controller manager.
    well the pods are not accessable its actally isolated, well to access the pod outside user so the need of service proxy is there.
        with the help of service proxy it talk to api server and he later talks to kubelet and then we can access the pod.

all these nodes and masters nodes located away communication with each other with CNI network(container network interface) eg. weav Net, Calico, etc.
kubectl talk to api server and keep the track of entire k8s cluster, 


way to create cluster:
    kubeadm: we get two or more ec2 and insall the kubeadm and connect them together in cluster.
    minikube(local/ec2):
    KIND Cluster: kubernetes in Docker this is full form, running k8s in docker.
    EKS/AKS/GKE: service provider managers the cluster.  
setup on AWS:
    connect with pem key on terminal.


    # ----------------------------
# 2. Install Kind (based on architecture)
# ----------------------------
if ! command -v kind &>/dev/null; then
  echo "📦 Installing Kind..."

  ARCH=$(uname -m)
  if [ "$ARCH" = "x86_64" ]; then
    curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.29.0/kind-linux-amd64
  elif [ "$ARCH" = "aarch64" ]; then
    curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.29.0/kind-linux-arm64
  else
    echo "❌ Unsupported architecture: $ARCH"
    exit 1
  fi

  chmod +x ./kind
  sudo mv ./kind /usr/local/bin/kind
  echo "✅ Kind installed successfully."
else
  echo "✅ Kind is already installed."
fi

# ----------------------------
# 3. Install kubectl (based on architecture)
# ----------------------------
if ! command -v kubectl &>/dev/null; then
  echo "📦 Installing kubectl (latest stable version)..."

  ARCH=$(uname -m)
  VERSION=$(curl -Ls https://dl.k8s.io/release/stable.txt)

  if [ "$ARCH" = "x86_64" ]; then
    curl -Lo ./kubectl "https://dl.k8s.io/release/${VERSION}/bin/linux/amd64/kubectl"
  elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
    curl -Lo ./kubectl "https://dl.k8s.io/release/${VERSION}/bin/linux/arm64/kubectl"
  else
    echo "❌ Unsupported architecture: $ARCH"
    exit 1
  fi

  chmod +x ./kubectl
  sudo mv ./kubectl /usr/local/bin/kubectl
  echo "✅ kubectl installed successfully."
else
  echo "✅ kubectl is already installed."
fi


sudo apt-get install docker.io


docker ps 
sudo user
this will return persmission error
so:
    sudo usermod -aG docker $USER && newgrp docker
now docker will work.
kubectl version
docker --version
kind --version


now the installation is done, lets do the configuration:
this is a yaml file:
this is a key pair thing but we can kay a key  before : and valude next after the :
also we can have a list in that claude with "-" and content of values and this goes vertically below with return button.
make a folder of kind-cluster with mkdir enter the folder.
now write config.yam

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: tws-cluster

nodes:
- role: control-plane
  image: kindest/node:v1.35.0
  extraPortMappings:
    - containerPort: 30080
      hostPort: 8080
      protocol: TCP

- role: worker
  image: kindest/node:v1.35.0

- role: worker
  image: kindest/node:v1.35.0

kind create cluster --name tws-cluster --config=config.yml

now lets see minikube:


Kubeadm:

baremetal meaning having two servers which means connecting both together master slave config.
in this setup we have one master node with kubeadm init,and we can add multiple worker nodes.

launch two instances Ec2 and ssh them in different terminals.
one master another worker.


what is containerd?
this is a container management tool,and it is used in docker as background.

expose port 6443 on ec2 security groups.

refer this for setup: https://github.com/LondheShubham153/kubestarter/blob/main/Kubeadm_Installation_Scripts_and_Documentation/README.md
run commands in both master and worker:

1. **Disable Swap**: Required for Kubernetes to function correctly.
    ```bash
    sudo swapoff -a
    ```

2. **Load Necessary Kernel Modules**: Required for Kubernetes networking.
    ```bash
    cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
    overlay
    br_netfilter
    EOF

    sudo modprobe overlay
    sudo modprobe br_netfilter
    ```

3. **Set Sysctl Parameters**: Helps with networking.
    ```bash
    cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
    net.bridge.bridge-nf-call-iptables  = 1
    net.bridge.bridge-nf-call-ip6tables = 1
    net.ipv4.ip_forward                 = 1
    EOF

    sudo sysctl --system
    lsmod | grep br_netfilter
    lsmod | grep overlay
    ```

4. **Install Containerd**:
    ```bash
    sudo apt-get update
    sudo apt-get install -y ca-certificates curl
    sudo install -m 0755 -d /etc/apt/keyrings
    sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
    sudo chmod a+r /etc/apt/keyrings/docker.asc

    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo \"$VERSION_CODENAME\") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

    sudo apt-get update
    sudo apt-get install -y containerd.io

    containerd config default | sed -e 's/SystemdCgroup = false/SystemdCgroup = true/' -e 's/sandbox_image = "registry.k8s.io\/pause:3.6"/sandbox_image = "registry.k8s.io\/pause:3.9"/' | sudo tee /etc/containerd/config.toml

    sudo systemctl restart containerd
    sudo systemctl status containerd
    ```

5. **Install Kubernetes Components**:
    ```bash
    sudo apt-get update
    sudo apt-get install -y apt-transport-https ca-certificates curl gpg

    curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg

    echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /' | sudo tee /etc/apt/sources.list.d/kubernetes.list

    sudo apt-get update
    sudo apt-get install -y kubelet kubeadm kubectl
    sudo apt-mark hold kubelet kubeadm kubectl
    ```

## Execute ONLY on the "Master" Node

1. **Initialize the Cluster**:
    ```bash
    sudo kubeadm init
    ```

2. **Set Up Local kubeconfig**:
    ```bash
    mkdir -p "$HOME"/.kube
    sudo cp -i /etc/kubernetes/admin.conf "$HOME"/.kube/config
    sudo chown "$(id -u)":"$(id -g)" "$HOME"/.kube/config
    ```

3. **Install a Network Plugin (Calico)**:
    ```bash
    kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.26.0/manifests/calico.yaml
    ```

4. **Generate Join Command**:
    ```bash
    kubeadm token create --print-join-command
    ```

> Copy this generated token for next command.

---

## Execute on ALL of your Worker Nodes

1. Perform pre-flight checks:
    ```bash
    sudo kubeadm reset pre-flight checks
    ```

2. Paste the join command you got from the master node and append `--v=5` at the end:
    ```bash
    sudo kubeadm join <private-ip-of-control-plane>:6443 --token <token> --discovery-token-ca-cert-hash sha256:<hash> --cri-socket 
    "unix:///run/containerd/containerd.sock" --v=5
    ```

    > **Note**: When pasting the join command from the master node:
    > 1. Add `sudo` at the beginning of the command
    > 2. Add `--v=5` at the end
    >
    > Example format:
    > ```bash
    > sudo <paste-join-command-here> --v=5
    > ```

---

## Verify Cluster Connection

**On Master Node:**

```bash
kubectl get nodes

```

   <img src="https://raw.githubusercontent.com/faizan35/kubernetes_cluster_with_kubeadm/main/Img/nodes-connected.png" width="70%">

---

## Verify Container Status on Worker Node
<img src="https://github.com/user-attachments/assets/c3d3732f-5c99-4a27-a574-86bc7ae5a933" width="70%">

kubernetes is mainly know for self-healing and scaling.
here not we understand we work with containers and followed by we make pods of it then deployment and then we create service then user may use it.
also if a container conatins nginx so it will provide that service while we can use same setup with few changes to make it a mysql conatiner pods then deployment and then service.

also like whats app groups here we have kubernetes groups.
this group is called namespace, this is a unit in which all the resources available is only for that group only.
this is a isolated unit not pod deployment services will disturd another.


command : kubectl get namespace or ns
defalut : this contains all the things which doesnot have a namespace assigned or created.
kube-node-lease : all kubernetes cluster nodes information goes in this kube-node-lease.
kube-public : all these public services goes in the kube-public
kube-system: system level services run on this.
local-path-storage : local storage pod.

kubectl get pods:
kublet get pods -n kube-system.


kubectl create ns nginx
  this creates the namespace.

kubectl get ns
  shows all namespace

kubectl run nginx --image=nginx
  create pod nginx

not the pod is created in defalut namespace
 now we can delete the pod kubectl delete pod nginx.

kubectl run nginx --image=nginx -n nginx
  this created the pod with namespace both nginx.

kubectl get pods -n nginx
  show the pod running in name space nginx.

this all we dont create in command line rather we use yaml.

not we create the yml file named namespace
  contains the kind,apiversion, metadata

and apply the changes to kubectl.
kubectl apply -f filename.yml
  we tell them take to api server and apply there.

now we create pod:
same it is pod.yml file conatins kind,apiversion,metadata(name,namespace),spec(containers(name,image,ports(containerPort)))
  pod file is ready.
  kubectl apply -f pod.yml

kubectl get pods -n nginx

now we can enter that pod commandline with command:
  kubeclt exec -it nginx-pod -n nginx -- bash
  also can check curl 127.0.0.1

lets make a deployment:
  same yml file with kind,apiversion,metadata(name,namespace),spec(replicas,selector(matchLables(app)),templet(metadata,lables(app)),spec(containers,name,image,ports(containerPort)))

  this give replicaset, statefulset, deployment.
  create replicas of pod or scale the pods for traffic.
  replicaset create pods based on the confic given eg, 4 pods needed to be created when traffic then this is called replicaset.
  statfulset: this gives numbering to pods, keep the pods numbers sequence so it is statefullset.
  deployment: same as replicaset but give addition rolling updates meaning this avoide downtime i guess by changing one pod at a time(updating)

what are label and selector:
  lables are basically labeling a pod,and in selectore when deployment tell them select the app which named as nginx or any if a match the do replica.

delete pod.

kubectl apply -f deployment.yml

scale deployment:
  kubectl scale deployment/nginx-deployment -n nginx --replicas=5
  this creates 5 pods

all these replicaset and statefulSet works the Replicas, but deployment does the rolling update.
kubectl get pods -n nginx -o wide
  this gives wide info

so the rolling update is something which is like to avoid downtime which is done with having said there are 5 pods already and we are changing the version then it will not change all 5 pods rather it will chance few pods so the traffic shift without downtime.
and slowely all pods is changes
below is the example we are running pods of nginx with nginx namespace so we will do version change.

command :
  kubectl set image deployment/nginx-deployment -n nginx nginx=nginx:1.27.3

check it is working with:
  kubectl get pods -n nginx

it state with few of then are running and new some says containercreating

this is creating pods and creating replicas of it and rolling updates.


topic: replicaset

lets delete the deployment file we created with command:
  kubectl delete -f deplayment.yml

replicaset file is exactly like the deployment file but with few changes which are:
   kind : ReplicaSet
   name: nginx-replicasets
  
to apply the replicaset file run command:
  kubectl apply -f replacaset.yml
now the replicaset is created

kubectl get replicaset -n nginx

replica uses the same technology which deplayment uses but deployment is little advance.
this does not provide us rolling update.

we will talking about the statefulsets later when learning storage now lets talk about the DaemoSets.

what daemonset does is that it makesure that at each node there must be one pod running.

all this deletion we were doing like above we did deployment deletion , now i ran kubectl delete -f replaset.yml this all is done in the nodes.

lets create a daemonset.yml
  vim daemonset.yml

or do:
  cp replicaset.yml daemonset.yml
and make some name changes and there is not replicas in specs 

now save and run:
  kubectl apply daemonset.yml

and check it with:
  kubectl get pods -n nginx 
  and
  kubectl get pods -n nginx -o wide

topics:
  lets do jobs and cron jobs:
jon is nothing but telling to complete a given or configured task done and job finished.
it can be backup, patching or 

kind: Job
apiVersion: batch/v1
metadata:
  name: demo-job 
  namespace: nginx
spec:
  completions: 1
  parallelism: 1 
  template:
    metadata:
      name: demo-job-pod 
      labels:
        app: batch-task
    spec:
      containers:
        - name: batch-container 
          image: busybox:latest 
          command:["sh", "-c" ,"echo Hello Dosto! && sleep 10"]
        restartPolicy: Never

applyit with :
  kubectl apply -f job.yml

check job with:
  kubectl get job -n nginx

check pods with:
  kubectl get pods -n nginx

it goes from pod created then running task then comepeted and then terminating atlast completed.

see the log of that pod with:
  kubectl logs pod/demo-job-asdsa -n nginx

delete the job

if you want a perticular job must run on schedule then the job is called cron job.
it follows a perticular schedule

create cron with:
vim cron.yml

apiVersion: batch/v1
kind: CronJob
metadata:
  name: hello
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello
            image: busybox:1.28
            imagePullPolicy: IfNotPresent
            command:
            - /bin/sh
            - -c
            - date; echo Hello from the Kubernetes cluster
          restartPolicy: OnFailure

write config and save

and apply

check pods , log, cron and delete




